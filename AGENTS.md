## Agent skills

### Issue tracker

项目规格和任务使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五阶段标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文领域文档布局：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
