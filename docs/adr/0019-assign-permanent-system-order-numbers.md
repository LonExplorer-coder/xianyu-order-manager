# 为原始订单分配永久系统订单编号

每笔原始订单在首次入库时获得形如 `20260813-000001` 的系统订单编号：日期取北京时间下的首次入库日期，后六位是当日递增序号。编号在当前数据目录内唯一，一经分配不再因来源更新、人工修改、生命周期变化或导出而改变。这使系统内部身份不依赖可修改或来自外部平台的字段，同时比随机标识更适合人工核对和按时间归档。

既有订单升级时按其首次入库时间和稳定次序确定性补齐。导出行号仅能表示单次文件中的位置，不作为原始订单身份；平台订单号继续作为平台业务身份，与系统订单编号并列保留。
